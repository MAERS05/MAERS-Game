/**
 * @file engine.js — test 游戏变体
 * @description 复制自 sq-du/base/engine.js，
 *   只需修改 1 行 AI 定制文件的 import 路径。
 *
 * TODO: 从 ../../../sq-du/base/engine.js 复制全文，
 *       将 AI import 改为指向本变体的 AI 定制文件：
 *
 *   - import { scheduleAI, scheduleAIRedecide, applyCustomization as applyTestAI }
 *       from '../ai/sq-du-test/ai-test.js';
 *
 *   - 将所有 applyMaesAI 改为 applyTestAI
 *
 *   同时将 constants.js 和 effect-handlers.js 的 import
 *   路径改为本变体目录下的版本。
 */
